import TicketTable from "@/components/TicketTable";

const Tickets: React.FC = () => {
    return (
        <div className="flex">
            <TicketTable />
        </div>
    );
};

export default Tickets;
